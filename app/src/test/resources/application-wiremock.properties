hentjournalsakinfo.url=http://localhost:${wiremock.server.port}/hentjournalsakinfo
aktoer.v2.endpointurl=http://localhost:${wiremock.server.port}/aktoerv2
sak.saker.url=http://localhost:${wiremock.server.port}/gsak
serviceGateway.url=http://localhost:${wiremock.server.port}/servicegw
securityTokenService.url=http://localhost:${wiremock.server.port}/sts
abac.pdp.endpoint.url=http://localhost:${wiremock.server.port}/abac
pensjonsakrs.v1.url=http://localhost:${wiremock.server.port}//pensjonsakrs

security.oidc.idp.sts.hostUrl=http://navStsHostUrl
security.oidc.idp.sts.issuerUrl=http://navStsIssuerUrl
security.oidc.idp.sts.loginUrl=${security.oidc.idp.sts.hostUrl}/oauth2/authorize
security.oidc.idp.sts.authorizationUrl=${security.oidc.idp.sts.issuerUrl}/oauth2/access_token
security.oidc.idp.sts.jwksUrl=http://navStsJwksUrl
